//
//  registerWebLogin.ts
//
//  Generated by Poll Castillo on 01/03/2023.
import { currentDate, customerName } from "./UserLoggedInfo.js";
import { _userAgent } from "./endpoints.js";
//
export const registerWebLogin = async () => {
    const $userAgent = _userAgent;
    const customer = await customerName();
    const creationDate = currentDate();
    const system = 'cliente';
    // User Agent *
    // Customer *
    // Creation date (day/month/year)
    // Creation time
    // System (platform type: 'cliente' or 'guardia')
    // User (the username)
    console.group('Data to reginster in Web access');
    console.info(customer);
    console.info(creationDate);
    console.log($userAgent);
    console.groupEnd();
};
